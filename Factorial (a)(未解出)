#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num,count=0;
    scanf("%d",&num);
    count=num;

    while(count>=1)
    {
        num=num*(num-1);
        printf("%d\n\n",num);
        count--;
    }
    printf("%d\n",num);
    return 0;
}
