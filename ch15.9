#include <stdio.h>
#include <stdlib.h>

template <class T>
T change(T a, T b, T c);

int main()
{
    int a[2];
    char b[2];
    double c[2];

    while(scanf("%d%d", &a[0], &a[1]) != EOF)
    {
        Scanf("%c%c", &b[0], &b[1]);
        scand("%lf%lf", &c[0], &c[1]);

        int change(a[0], a[1], 0);
        printf("%d %d\n", a[0], a[1]);

        char change(b[0], b[1], a);
        printf("%c %c\n", b[0], b[1]);

        double change(c[0], c[1], 6.6);
        printf("%.3lf %.3lf\n", c[0], c[1]);


    }

    return 0;
}


template <class T>
T change(T a, T b, T c)
{
    c = a;
    a = b;
    b = c;
}

